<div class="" id="app2">
    <div class="row">
        <div class="col-lg-12">
            <div class="card-box">
                <h4 class="header-title">Tags Report</h4>
                <p class="sub-header">
                    This will show the report of all Tags
                </p>

                <div class="table-responsive">
                    <table class="table mb-0">
                        <thead class="thead-dark">
                            <tr>
                                <th>#</th>
                                <th>Product Name</th>
                                <th>Product Serial</th>
                                <th>Tag Number</th>
                                
                                <th>Action</th>

                                <!-- <th>Date Created</th> -->
                            </tr>
                        </thead>
                        <tbody>
                            <tr v-for="tag in tags">
                                <th scope="row">{{ tag.id }}</th>
                                <td>{{ tag.product_name }}</td>
                                <td>{{ tag.product_serial }}</td>
                                <td>{{ tag.tag_number }}</td>
                              
                                <td>
                                    <button type="button" class="btn btn-primary waves-effect waves-light">Update</button>
                                    <button type="button" class="btn btn-success waves-effect waves-light">Delete</button>
                                </td>
                                <!-- <td>{{ tag.createdAt }}</td> -->
                            </tr>
                       
                        </tbody>
                    </table>
                </div> <!-- end table-responsive-->

            </div> <!-- end card-box -->
        </div> <!-- end col -->
    </div>
</div>

<script>
        var app = new Vue({
            el: '#app2',
            data: {
                tags:[]
            },
            async asyncData ({}) {
                // let allTags  = await axios.get('/api/tags/')
                // console.log("allTags")
                // return { tags: allTags }

                console.log("dd")
            },
            async mounted(){
                tags  = await axios.get('/api/tags/')
                this.tags = tags.data
            },
            methods: {
                reverseMessage: function () {
                    this.message = this.message.split('').reverse().join('')
                },
            
            }
        })
    
    </script>